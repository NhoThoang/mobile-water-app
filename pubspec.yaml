name: water_billing_app
description: A water billing and meter reading application.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  dio: ^5.3.3
  flutter_secure_storage: ^9.0.0
  image_picker: ^1.0.4
  intl: ^0.18.1
  google_fonts: ^6.1.0
  sqflite: ^2.3.0
  path: ^1.8.3
  path_provider: ^2.1.1
  logger: ^2.0.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
